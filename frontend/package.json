{
  "name": "securevote-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2",
    "crypto-js": "^4.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--no-deprecation react-scripts start 2>&1 | node -e \"process.stdin.resume();process.stdin.setEncoding('utf8');let b='';process.stdin.on('data',d=>{b+=d;let l=b.split('\\n');b=l.pop();l.forEach(x=>{if(!x.match(/npm run build|Note that the development|To create a production/))console.log(x)});});process.stdin.on('end',()=>{if(b&&!b.match(/npm run build|Note that the development|To create a production/))console.log(b);});\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:5000"
}